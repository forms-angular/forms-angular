# forms-angular

> forms-angular (sometimes fng) is a full stack library for the MEAN stack that simplifies many aspects of form based applications.

- The application is build using Angular JS and Typescript
- The repo contains the website for the library, and the library itself.
- The server for the website is started by running `node server.js` in the website directory.  It is served at `localhost:9000`.
